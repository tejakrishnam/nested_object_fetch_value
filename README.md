# nested_object_fetch_value

# Input :
A string in a dictionary format
- Number of keys to be provided.
- keys(Press Enter after each input key is provided).

# Output:
- Value(The value of the key provided).
